{
  "name": "nestjs-kit",
  "version": "1.0.0",
  "private": true,
  "description": "Reusable packages for NestJS projects",
  "license": "MIT",
  "engines": {
    "pnpm": ">=11 <12"
  },
  "devDependencies": {
    "typescript": "5.7.2"
  },
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
