{
  "name": "@nestjs-kit/prisma",
  "version": "1.0.0",
  "private": true,
  "description": "Reusable Prisma integration for NestJS",
  "license": "MIT",
  "peerDependencies": {
    "@nestjs/common": ">=11.0.0",
    "@nestjs/core": ">=11.0.0",
    "@prisma/client": ">=6.0.0 <7.0.0"
  },
  "devDependencies": {
    "@nestjs/common": "11.2.1",
    "@nestjs/core": "11.2.1",
    "@prisma/client": "6.0.0",
    "@types/express": "5.0.0",
    "prisma": "6.0.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "pnpm build",
    "clean": "rm -rf dist",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'No tests yet'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
